@AGENTS.md

## Start here

New to this repo? Read **`docs/PROJECT_STATE.md`** — what the app is, where the
core code lives, the data pipeline, current state, and the critical gotchas
(dev-build-not-Expo-Go, the dependency traps, club_facts.json, babel config).
Then `docs/Major Overhaul + Bug fixes.md` (systems as-built) and
`docs/More Competitions & Modes.md` (roadmap + caveats).
